import Search from '../search/Search';
import './index.css';

function Header() {
    return (
        <header className="headerContainer">
            <Search />
        </header>
    );
}

export default Header;